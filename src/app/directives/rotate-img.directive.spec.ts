import { RotateImgDirective } from './rotate-img.directive';

describe('RotateImgDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateImgDirective();
    expect(directive).toBeTruthy();
  });
});
