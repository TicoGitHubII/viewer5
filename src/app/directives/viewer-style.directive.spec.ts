import { ViewerStyleDirective } from './viewer-style.directive';

describe('ViewerStyleDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ViewerStyleDirective();
  //   expect(directive).toBeTruthy();
  // });
});
